import 'package:flutter/material.dart';

class RecordsScreen extends StatelessWidget {
  RecordsScreen({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Records Screen'),
    );
  }
}
